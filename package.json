{
  "name": "doodlehub",
  "private": true,
  "scripts": {
    "install": "cd server && npm install",
    "build": "cd my-app && npm install && npm run build",
    "start": "node server/index.js"
  }
}
